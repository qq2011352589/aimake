# task.md — 任务清单

> 创建时间：2026-08-10 ｜ 更新：2026-08-10 ｜ 状态：**全部里程碑完成（M1-M5 ✅）**
> 依据：plan.md 实施阶段 0~5

## 状态图例

- `[ ]` 待办 ｜ `[~]` 进行中 ｜ `[x]` 已完成 ｜ `[-]` 已取消 ｜ `[!]` 受阻

## 任务列表

### 里程碑 M0：准备（阶段 0）

| # | 任务 | 状态 | 优先级 | 负责人 | 依赖 | 验收标准 |
|---|------|------|--------|--------|------|----------|
| T1 | 初始化项目结构与入口（aimake.py / 包结构 / README / LICENSE） | [x] | 高 | 用户 | 无 | 仓库结构与 README/LICENSE 就绪；入口空跑并入 T3 |
| T2 | 验证 Termux 上 codex exec / opencode run 可用 | [~] | 高 | 用户 | 无 | 能执行一次 `codex exec --version` 与 `opencode run --help` |

### 里程碑 M1：生成器（阶段 1）

| # | 任务 | 状态 | 优先级 | 负责人 | 依赖 | 验收标准 |
|---|------|------|--------|--------|------|----------|
| T3 | 目录遍历 + ignore 清单（排除 .git/node_modules/__pycache__/dist/build/.aimake） | [x] | 高 | — | T1 | 对测试项目输出可见目录树，忽略规则生效 |
| T4 | 构建三类边：目录树（树边）+ 依赖候选名单（依赖边）+ 捷径表来源分析 | [x] | 高 | — | T3 | 生成依赖图可拓扑排序；候选名单为纯目录名列表 |
| T5 | 知识根镜像骨架创建 + `.meta` 指纹格式 | [x] | 高 | — | T3 | 知识根 `.aimake/<目标>/` 按目录镜像生成骨架；.meta 记录文件清单 hash |
| T6 | 生成提示词模板（统一 schema 十小节，中文，含内容分级：轻量/全量两档） | [x] | 高 | — | T4 | 模板可注入目录文件/子级摘要/候选名单；低复杂度目录走轻量档 |
| T7 | 叶子节点并行调用 codex exec（并发上限 + 超时 + 失败重试） | [x] | 高 | — | T5,T6 | 叶子 agents.md 生成成功，超时/失败有标记不阻塞 |
| T8 | 父级聚合生成（注入子级摘要 + 依赖候选名单） | [x] | 高 | — | T7 | 父级 SUB-KNOWLEDGE 含子级摘要+路径，schema 可解析 |
| T9 | 根节点生成（聚合 + 跨目录契约 + 全局捷径路由表 + .aimake-link） | [x] | 高 | — | T8 | 根 agents.md 含全局 WHERE TO LOOK 捷径表；可选 .aimake-link 指针 |
| T10 | 两阶段生成（依赖缺失标记 → 快照补一轮） | [x] | 中 | — | T9 | 有跨目录依赖缺失时，第二轮补生成成功 |
| T11 | 自举：aimake 在自己的仓库上跑 init（知识根镜像） | [x] | 中 | — | T10 | aimake 仓库在知识根生成完整镜像树，无风暴/无死锁 |

### 里程碑 M2：消费与更新（阶段 2）

| # | 任务 | 状态 | 优先级 | 负责人 | 依赖 | 验收标准 |
|---|------|------|--------|--------|------|----------|
| T12 | `update [路径]`：指纹对比 → 受影响子图（本目录 + 祖先链 + DEPENDS 消费者） | [x] | 高 | — | T10 | 改一个文件，只重生成受影响链，其余不动 |
| T13 | `status`：过期清单 + 待处理反馈计数 | [x] | 高 | — | T12 | 输出过期目录列表，幂等 |
| T14 | `tree`：知识树总览（全局索引物化） | [x] | 中 | — | T9 | 输出树形结构 + 各节点一句话摘要 |
| T15 | `ask`：QA 条目/捷径表确定性匹配 + 来源标注 | [x] | 中 | — | T9,T13 | 命中返回「答案+证据指针」，未命中给导航建议 |
| T16 | 符号自检（通道 3）：KEY SYMBOLS / QA 证据指针 vs 源码 grep | [x] | 中 | — | T13 | status 免费输出失效符号清单 |

### 里程碑 M3：反馈闭环（阶段 3）

| # | 任务 | 状态 | 优先级 | 负责人 | 依赖 | 验收标准 |
|---|------|------|--------|--------|------|----------|
| T17 | 反馈文件格式（`.aimake/feedback/<日期>-<目录>.md`：错误小节 + 证据 + 来源条目） | [x] | 高 | — | T11 | 示例反馈文件可被解析 |
| T18 | 四方确认逻辑（消费者/消费者父目录/owner/owner 父目录仲裁） | [x] | 高 | — | T17 | 四票汇聚，仲裁逻辑可测试 |
| T19 | 阈值触发（≥2 票 / 任一事实错误 / 自检失败） | [x] | 中 | — | T18 | 阈值判定有单测覆盖 |
| T20 | `update --feedback`：决策 → 重生成（反馈注入提示词）→ 连锁更新父级摘要 | [x] | 高 | — | T19 | 反馈处理全流程跑通，父级摘要同步更新 |
| T21 | update 报告写入 tasks.md（更新历史可跨会话查） | [x] | 低 | — | T20 | tasks.md 记录"更新了哪些节点" |

### 里程碑 M4：工程化（阶段 4）

| # | 任务 | 状态 | 优先级 | 负责人 | 依赖 | 验收标准 |
|---|------|------|--------|--------|------|----------|
| T22 | 防风暴测试：.aimake 双向排除 / symlink 环 / 指纹幂等 / 超时降级 | [x] | 高 | — | T11 | 四道闸各有测试用例通过 |
| T23 | 上下文预算机制（节点封顶 + 降级策略） | [x] | 中 | — | T6 | 超预算时按策略降级，成本线性可测 |
| T24 | Nuitka 编译（Termux：patchelf/ccache/binutils/ldd/termux-elf-cleaner，≥2.6.7） | [x] | 高 | — | T22,T23 | Termux 上产出可运行独立二进制 |
| T25 | 文档完善（README 补 CLI 用法 + 消费协议） | [x] | 低 | — | T24 | 文档与设计一致 |

### 里程碑 M5：项目生成（阶段 5）

| # | 任务 | 状态 | 优先级 | 负责人 | 依赖 | 验收标准 |
|---|------|------|--------|--------|------|----------|
| T26 | `scaffold` 命令骨架：一句话 → 提案生成（codex exec 一轮，输出 proposal.md） | [x] | 高 | — | T2,T24 | `scaffold "创建数学大厦"` 产出 proposal.md |
| T27 | 提案确认交互（默认确认 / `--default` 快速模式） | [x] | 中 | — | T26 | 确认流程可交互、可跳过 |
| T28 | 按目录粒度生成源码（复用 init 并行/超时/重试机制） | [x] | 高 | — | T27 | 生成目录树与源码，遵循 ignore 规则 |
| T29 | 生成骨架：plan.md / task.md / README.md / proposal.md | [x] | 中 | — | T28 | 骨架四件套齐全，人可读 |
| T30 | 生成后自动跑 init（知识树与项目同生） | [x] | 高 | — | T28 | 生成的项目自带 .aimake 知识树 |
| T31 | 验证：scaffold 生成自己 → 对自己跑 init → 自举闭环 | [x] | 中 | — | T30 | 自举全链路无风暴、无死锁 |

## 进行中

- 当前任务：无（全部里程碑完成）
- 阻塞项：codex exec 认证（真实引擎接入后需用真实引擎重验各生成命令）

## 已完成记录

| 日期 | 任务 | 说明 |
|------|------|------|
| 2026-08-10 | #T1 | 项目结构/README/LICENSE 就绪；`python -m aimake` 待实现（空跑验证延后） |
| 2026-08-10 | #T3 | 目录遍历 + ignore 清单完成：默认 6 项排除 + .aimakeignore 自定义 + fnmatch 通配；`scan` 子命令可验收（测试项目验证通过） |
| 2026-08-10 | #T4 | 三类边构建完成：树边（目录树）+ 依赖候选（多语言 import 扫描：Python/JS/C/Rust/C#/Go/Java）+ 后序拓扑序；`scan --deps` 可验收 |
| 2026-08-10 | #T5 | 知识根镜像骨架 + .meta 指纹完成：`init` 在父目录运行生成 `.aimake/<目标>/` 镜像 + 每目录指纹 + dry-run 计划；is_stale 过期检测验证通过 |
| 2026-08-10 | #T6 | 生成提示词模板完成：全量档（十小节 schema）+ 轻量档（SUMMARY 级）+ 内容分级（<10 文件无子目录→轻量）；extract_overview 聚合提取；init dry-run 显示分级标签 |
| 2026-08-10 | #T7 | 生成引擎抽象 + 叶子并行完成：engine.py（通用接口，codex/opencode 预置，.aimake/aimake.json 用户配置）+ runner.py（并发/超时/重试/失败标记）+ mock 引擎；init --engine mock 跑通叶子生成全链路 |
| 2026-08-10 | #T8 | 父级聚合生成完成：分层波浪（叶子→父级→根，按距叶子高度分组）；父级读子级 agents.md 提取 OVERVIEW 注入 SUB-KNOWLEDGE；mock 端到端验证 5/5 |
| 2026-08-10 | #T9 | 根节点增强完成：根 prompt 含全局捷径表 + 跨目录契约要求；.aimake-link 指针写入目标项目根（相对路径正确） |
| 2026-08-10 | #T10 | 两阶段生成完成：阶段一波浪 + 阶段二失败重试 + 祖先链快照刷新（高度表全量预计算）；flaky 引擎验证 4/4 收敛 |
| 2026-08-10 | #T11 | 自举验证完成：aimake 仓库根跑 init（target==cwd）→ 2 节点镜像树；.aimakeignore 自定义规则生效（.omo 排除）；重复跑幂等无风暴 |
| 2026-08-10 | #T12 | update 完成：指纹对比 → 受影响子图（过期 + 祖先链 + DEPENDS 消费者反向索引）；.aimake-link 先于 walk 写入；重生成后刷新 .meta；幂等复跑验证通过 |
| 2026-08-10 | #T13 | status 完成：过期清单（is_stale 只读判定）+ 待处理反馈计数（.aimake/feedback/）+ 未初始化提示；幂等验证通过 |
| 2026-08-10 | #T14 | tree 完成：树形总览 + 一句话摘要（extract_overview）+ 过期/依赖标记 + 根 WHERE TO LOOK 捷径表物化 |
| 2026-08-10 | #T15 | ask 完成：QA 条目确定性匹配（中文 bigram 分词）+ 来源标注 + 捷径表导航 + 系统性否定；三种场景验证通过 |
| 2026-08-10 | #T16 | 符号自检（通道 3）完成：KEY SYMBOLS 缺失 + QA 证据行号越界 vs 源码比对，零 token 集成 status；验证无误报 |
| 2026-08-10 | #T17-#T21 | M3 反馈闭环完成：反馈文件格式+解析、四方确认+阈值（≥2 票/任一事实错误/自检失败）、update --feedback 注入重生成+连锁、tasks.md 更新报告；三分支验证通过 |
| 2026-08-10 | #T22 | 防风暴测试套件完成（tests/，unittest 零依赖）：双向排除/不回流、symlink 环不跟随、指纹幂等/增删/mtime 不触发、超时降级/失败不阻塞；12/12 通过 |
| 2026-08-10 | #T23 | 上下文预算机制完成：build_prompt_budgeted 三级降级阶梯（子级摘要去文本→文件清单截断→降轻量档）+ load_budget 配置 + --budget 覆盖 + 降级报告；单测与 CLI 验证通过 |
| 2026-08-10 | #T24 | Nuitka 编译完成：main.py 入口 + --standalone --onefile → bin/aimake.bin（3.6M）；glibc PATH 补 patchelf；启动器 bin/aimake 内置 LD_LIBRARY_PATH 解决 libpython 链接；mock 全链路验证通过 |
| 2026-08-10 | #T25 | 文档完善完成：README 重写（快速开始/CLI 用法/消费协议/目录结构/测试）+ AGENTS.md 构建命令与 Termux 坑更新；CLI 参数逐一核对一致 |
| 2026-08-10 | #T26-#T31 | M5 scaffold 完成：一句话→提案→确认（--default）→按目录生成源码（manifest 解析）→骨架四件套→自动 init（知识树同生）；自举闭环验证（status/update 幂等/ask）；mock 全链路通过 |

---

**维护约定**：开始任务前先标记 `[~]`；完成即标 `[x]` 并移入「已完成记录」；计划变更时同步更新 plan.md。
