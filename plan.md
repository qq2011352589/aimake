# plan.md — 项目计划

> 创建时间：2026-08-10 ｜ 更新：2026-08-10 ｜ 状态：**规划完成，待实施**
> 设计依据：AGENTS.md「核心设计」（经多轮讨论沉淀）

## 1. 目标 (Goals)

- **目标 1**：实现 `aimake init`——为任意项目 A 递归生成 `.aimake/agents.md` 分层知识树（自底向上，按目录粒度调用 `codex exec` / `opencode run`）。
- **目标 2**：实现消费与更新闭环——`update`（指纹驱动局部重生成）、`status`（过期清单 + 符号自检）、`tree`（全局索引）、`ask`（QA 问答）。
- **目标 3**：实现反馈驱动更新——`update --feedback`（事实性错误报告 → 四方确认 → 重生成），让"知识错了"也能被修正。
- **目标 4**：工程化交付——纯标准库实现、Nuitka 编译为 Termux 可运行的独立可执行文件、防风暴四道闸全部落实。
- **目标 5**：实现 `aimake scaffold "一句话"`——从自然语言描述生成项目骨架（提案确认 → 按目录生成源码 → 自动 init 知识树），生成的项目自带 plan.md / task.md / proposal.md。
- **非目标**：不拦截/不参与对话（消费方 AI 直接读知识树）；不联网查证项目外知识；不训练/微调任何模型；不做 GUI/IDE 插件。

## 2. 范围 (Scope)

| 项 | 内容 |
|----|------|
| 包含 | CLI（init/update/status/tree/ask/scaffold）、目录遍历 + ignore 清单、三类边构建（树边/依赖边/捷径边）、依赖发现（import 候选 + 模型确认）、.meta 指纹、反馈队列、全局捷径路由表（WHERE TO LOOK）、统一 schema 生成提示词模板、scaffold 项目生成（提案→源码→init）、自举（aimake 跑自己） |
| 不包含 | 向量库/RAG、模型训练、联网查询、Web 服务、多项目并行管理 |
| 依赖 | `codex exec` 或 `opencode run`（生成引擎，可替换）；Python 3 标准库（零第三方）；Nuitka（仅构建期） |

## 3. 里程碑 (Milestones)

| 里程碑 | 交付物 | 完成标准 | 截止 |
|--------|--------|----------|------|
| M1：生成器 | `init` 全树生成可用 | 对测试项目 A 生成完整知识树，叶子并行/父级聚合/根节点契约，报告含调用次数与耗时 | （待定） |
| M2：消费与更新 | `update` / `status` / `tree` / `ask` | 指纹驱动局部重生成 + 连锁更新；tree 输出全局索引（捷径表物化）；ask 命中 QA/捷径表即答（带来源） | （待定） |
| M3：反馈闭环 | `update --feedback` | 反馈文件 → 四方确认 → 决策重生成 → 连锁更新，全流程跑通 | （待定） |
| M4：工程化 | Nuitka 可执行 + 防风暴测试 | Termux 上编译运行；四道闸（双向排除/symlink 防环/幂等/超时）均有测试覆盖 | （待定） |
| M5：项目生成 | `scaffold "一句话"` | 一句话 → 提案 → 用户确认 → 按目录生成源码 → 自动 init 知识树；生成骨架自带 plan.md/task.md/proposal.md | （待定） |

## 4. 实施阶段 (Phases)

### 阶段 0：准备
- [x] 确定技术栈（Python 3 + Nuitka，已定）
- [ ] 初始化项目结构与入口（aimake.py / 包结构）
- [ ] 验证 Termux 上 codex exec / opencode run 可用

### 阶段 1：生成器（M1）
- [ ] 目录遍历 + ignore 清单（排除 .git/node_modules/__pycache__/dist/build/.aimake）
- [ ] 构建三类边：目录树（树边）+ 依赖候选名单（依赖边）+ 捷径表来源分析（语义路由）
- [ ] 知识根镜像骨架创建（运行目录 `.aimake/` + 目标镜像 + `.meta` 指纹格式）
- [ ] 生成提示词模板（统一 schema：OVERVIEW/SUB-KNOWLEDGE/DEPENDS/FILES/WHERE TO LOOK/QA/KEY SYMBOLS/COMMANDS/ANTI-PATTERNS/EXTERNAL）
- [ ] 叶子节点并行调用 codex exec（并发上限 + 超时）
- [ ] 父级聚合生成（注入子级摘要 + 依赖候选名单）
- [ ] 根节点生成（聚合 + 跨目录契约 + 全局捷径路由表）
- [ ] 两阶段生成（依赖缺失标记 → 快照补一轮）
- [ ] 内容分级（低复杂度目录轻量 agents.md，高复杂度全 schema）+ 同级差异化摘要
- [ ] 子树分批 init（`aimake init <子路径>`）+ 运行位置/扫描目标语义
- [ ] `.aimake-link` 指针文件（可选：消费发现协议）
- [ ] 自举：aimake 在自己的仓库上跑 init

### 阶段 2：消费与更新（M2）
- [ ] `update [路径]`：指纹对比 → 受影响子图（本目录 + 祖先链 + DEPENDS 消费者）
- [ ] `status`：过期清单 + 待处理反馈计数
- [ ] `tree`：知识树总览输出（全局索引）
- [ ] `ask`：QA 条目确定性匹配 + 来源标注
- [ ] 符号自检（通道 3）：KEY SYMBOLS / QA 证据指针 vs 源码 grep 比对

### 阶段 3：反馈闭环（M3）
- [ ] 反馈文件格式（`.aimake/feedback/<日期>-<目录>.md`：错误小节 + 证据 + 来源条目）
- [ ] 四方确认逻辑（消费者 / 消费者的父目录 / owner / owner 的父目录仲裁）
- [ ] 阈值触发（≥2 票 / 任一事实错误 / 自检失败）
- [ ] `update --feedback`：决策 → 重生成（反馈注入提示词）→ 连锁更新父级摘要
- [ ] update 报告写入 tasks.md（更新历史可跨会话查）

### 阶段 4：工程化收尾（M4）
- [ ] 防风暴测试：.aimake 双向排除、symlink 环、指纹幂等（跑两次 = 跑一次）、超时降级
- [ ] 上下文预算机制（节点封顶 + 降级策略）
- [ ] Nuitka 编译（Termux：patchelf/ccache/binutils/ldd/termux-elf-cleaner，版本 ≥ 2.6.7）
- [ ] 文档完善（README + 消费协议）

### 阶段 5：项目生成（M5）
- [ ] `scaffold` 命令骨架：一句话 → 提案生成（codex exec 一轮，输出 proposal.md）
- [ ] 提案确认交互（默认确认 / `--default` 快速模式）
- [ ] 按目录粒度生成源码（复用 init 的并行/超时/失败重试机制）
- [ ] 生成骨架：plan.md / task.md / README.md / proposal.md（人维护 md 哲学）
- [ ] 生成后自动跑 init（知识树与项目同生）
- [ ] 验证：scaffold 生成自己 → 对自己跑 init → 自举闭环

## 5. 风险与对策 (Risks)

| 风险 | 影响 | 概率 | 对策 |
|------|------|------|------|
| codex exec 认证/成本 | 生成不可用或 token 费用失控 | 高 | 生成前预算估算；每节点上下文封顶；并发上限；可切换 opencode run |
| 模型输出不稳定（schema 走样） | agents.md 不可被父级解析 | 中 | 提示词模板强约束 + 生成后 schema 校验 + 失败重试 |
| 依赖发现召回不足（隐性依赖漏检） | DEPENDS 缺失，链路断裂 | 中 | 多通道发现（import 候选 + 模型识别配置/运行时）；反馈通道补漏 |
| 反馈软肋（消费方不写反馈） | 纠错退化靠人工 | 中 | 写进消费协议（软约束）；符号自检零成本兜底 |
| Termux 编译坑（Nuitka 版本/libpython） | 构建失败 | 低 | 固定版本 ≥ 2.6.7；按 AGENTS.md NOTES 装齐工具链 |
| 大项目成本爆炸（千级目录） | init 调用次数过多 | 低 | 上下文预算线性约束；ignore 清单；内容分级（轻量/全量）；子树分批 init |
| 一句话需求模糊 → 生成不符预期 | scaffold 产出偏离用户意图 | 中 | 提案确认环节（`--default` 可跳过）；proposal.md 留档可追溯；生成质量差走反馈通道 |

## 6. 决策记录 (Decisions)

| 日期 | 决策 | 理由 | 提出人 |
|------|------|------|--------|
| 2026-08-10 | 产物内容一律中文，知识文件后缀一律 `.md` | 面向中文用户与 AI 的确定性约定；schema 键保持英文协议键，中文化须全局一致迁移 | 用户 |
| 2026-08-10 | 知识图 = 树边 + 依赖边（DAG）；依赖边只存指针 + 摘要，不复制知识 | 上下文严格线性，杜绝无意义增长 | 讨论 |
| 2026-08-10 | owner 语义：agents.md 是该目录知识权威管理者（所有权/边界守卫/委托授权） | 消费/查询/更新责任链与目录层级一致 | 讨论 |
| 2026-08-10 | 更新三通道：指纹（文件变）/ 反馈（知识错）/ 符号自检（零 token），全部显式触发 | 指纹抓不到"知识错了"；反馈必须事实性报告，禁主观评分 | 讨论 |
| 2026-08-10 | 两阶段生成 + 快照引用；父目录只决策不改写 | 防跨目录死锁；父级无子级 ground truth，直接改 = 以错改错 | 讨论 |
| 2026-08-10 | 禁止动态 agent 互问（codex exec 嵌套调用）——静态读文件是唯一合法的"问" | 防递归风暴与成本爆炸 | 讨论 |
| 2026-08-10 | 技术栈：Python 3 + Nuitka（已定） | 纯标准库零依赖；Nuitka 官方支持 Termux 且真编译；GNU make 仅作原型演示 | 讨论 |
| 2026-08-10 | 语言约定：代码英文标识符，人/AI 可见文本全中文 | AI 维护源码、人维护 md 的分工；产物一致性 | 讨论 |
| 2026-08-10 | 知识图拓扑：树边 + 依赖边 + 捷径边（消费视图） | 树保证边界与权威，依赖边管跨目录契约，捷径边让模糊问题一跳直达；捷径边不参与生成序与失效传播 | 讨论 |
| 2026-08-10 | scaffold（M5）：项目生成与知识生成分离为两级流程 | 防嵌套生成风暴；知识层完全复用现有 init 设计 | 讨论 |
| 2026-08-10 | scaffold 生成骨架自带 plan.md / task.md / proposal.md | 人维护 md 哲学；proposal 留档可追溯 | 讨论 |
| 2026-08-10 | 知识根模型（单一模式）：知识根 = 运行目录 .aimake/，目标项目镜像，无逐目录 .aimake | 大项目零侵入 + 知识独立成项目 + 多项目管理，一条规则统一（运行位置即布局） | 讨论 |
| 2026-08-10 | 内容分级：低复杂度节点轻量 agents.md，高复杂度全 schema | 大项目成本策略，避免千级同重文件 | 讨论 |
